# Claude-Specific Instructions

Read [`AGENTS.md`](./AGENTS.md) for primary project instructions.

This repository is a Claude Code marketplace for the `compound-wordpress-engineering` plugin —
a **WordPress overlay** for Every's `compound-engineering`. Add WordPress-specific work on top
of upstream; never re-vendor upstream's generic `ce-*` agents, skills, or commands. See
`AGENTS.md` for the overlay model, repository structure, and metadata-sync rules.
